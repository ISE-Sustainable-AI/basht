apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test
spec:
  storageClassName: standard #TODO make this a config thing
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
